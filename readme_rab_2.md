# 📦 Инструмент визуализации графа зависимостей (Вариант №9)

Данный проект реализует **вариант №9** из методички по дисциплине *«Конфигурационное управление»*. Цель работы — создать CLI-инструмент на Python, который:

- читает XML-конфигурацию,
- получает зависимости Rust-пакета (Cargo),
- строит граф зависимостей (включая транзитивные),
- поддерживает тестовый режим с произвольным графом из файла,
- выводит обратные зависимости,
- выполняет визуализацию в формате **D2 → PNG**.

Проект полностью реализует **Этапы 1–5** согласно требованиям.

---

## 📁 Структура проекта

```
depgraph_rust/
│   main.py                # основной код CLI-приложения
│   config_cargo.xml       # конфиг для Этапа 2 (работа с Cargo.toml)
│   config_test.xml        # конфиг для Этапов 3-5 (работа с test_graph)
│   Cargo.toml             # тестовый файл зависимостей (этап 2)
│   test_graph.txt         # тестовый граф (этапы 3–5)
│   graph.png              # будет создан на этапе 5
│   graph.d2               # D2-описание графа
```

---

# 🔧 Установка зависимостей

Перед запуском установите зависимости:

```bash
pip install requests tomli
```

(если установлен Python 3.11+ — `tomllib` уже встроен, `tomli` можно не ставить)

---

# 📘 Конфигурационные файлы

## 1. **config_cargo.xml** (для Этапа 2)

Используется для анализа настоящего Cargo.toml.

```xml
<?xml version="1.0" encoding="UTF-8"?>
<config>
    <packageName>your_crate_name</packageName>
    <repositoryUrl>./Cargo.toml</repositoryUrl>
    <useTestRepository>false</useTestRepository>
    <outputImage>graph_cargo.png</outputImage>
    <filterSubstring></filterSubstring>
</config>
```

## 2. **config_test.xml** (для Этапов 3–5)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<config>
    <packageName>A</packageName>
    <repositoryUrl>./test_graph.txt</repositoryUrl>
    <useTestRepository>true</useTestRepository>
    <outputImage>graph.png</outputImage>
    <filterSubstring></filterSubstring>
</config>
```

---

# 📜 Описание файлов

## ✔ Cargo.toml (для этапа 2)

```toml
[package]
name = "your_crate_name"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
rand = "0.8"
tokio = { version = "1", features = ["full"] }
reqwest = "0.11"
```

## ✔ test_graph.txt (для этапов 3–5)

```
# Тестовый граф для варианта 9
A: B C
B: C D
C: D
D:
E: A D
```

---

# 🚀 Запуск проекта

## 🔹 ЭТАП 1 — Чтение XML-конфига
```bash
python main.py --config config_test.xml --stage 1
```

---

## 🔹 ЭТАП 2 — Получение зависимостей из Cargo.toml
```bash
python main.py --config config_cargo.xml --stage 2
```
Вывод будет примерно:
```
Прямые зависимости (из Cargo.toml):
 - serde
 - rand
 - tokio
 - reqwest
```

---

## 🔹 ЭТАП 3 — Построение графа зависимостей (тестовый режим)
```bash
python main.py --config config_test.xml --stage 3
```
Выведет BFS-обход.

---

## 🔹 ЭТАП 4 — Обратные зависимости
```bash
python main.py --config config_test.xml --stage 4
```
Покажет какие узлы зависят от выбранного.

---

## 🔹 ЭТАП 5 — Визуализация графа (D2 → PNG)
```bash
python main.py --config config_test.xml --stage 5
```
Сгенерирует:
- `graph.d2` — текстовое описание
- `graph.png` — картинку

> Если утилита `d2` не установлена, изображение нужно будет сгенерировать вручную:
> ```bash
> d2 graph.d2 graph.png
> ```

---

# 📚 Этапы работы по методичке

## 🧩 Этап 1 — минимальный прототип
- чтение XML
- вывод параметров в формате ключ=значение
- обработка ошибок конфигурации

## 🧩 Этап 2 — сбор данных
- анализ Cargo.toml
- получение прямых зависимостей

## 🧩 Этап 3 — основные операции
- построение графа зависимостей
- BFS с рекурсией
- фильтрация узлов
- обработка циклов

## 🧩 Этап 4 — дополнительные операции
- поиск обратных зависимостей
- вывод BFS-порядка

## 🧩 Этап 5 — визуализация
- генерация D2
- вывод дерева зависимостей
- сохранение PNG

---

## 📝 Заключение
Проект полностью реализует **вариант №9** практической работы по Конфигурационному управлению. Все этапы оформлены, код структурирован, имеются конфигурации для разных режимов работы и файлы тестовых данных.

